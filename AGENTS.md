# Content Hub

- Canonical metadata lives in `PROJECT.md`.
- Required reading: `PROJECT.md`, `README.md`, `docs/platform-docs.md`, `src/app.jsx`, and the relevant feature area under `src/features/`.
- Simplification is the current product priority.
- Treat `supabase/migrations/` and `.env*` as protected unless the task explicitly requires them.
