{
  "session_id": "86c5ced9-db0e-4860-bac1-fd94d83fef20",
  "transcript_path": "/Users/dan/.claude/projects/-Users-dan-dev-population-matters-tools-Content-Hub/86c5ced9-db0e-4860-bac1-fd94d83fef20.jsonl",
  "hook_event_name": "PreToolUse",
  "tool_name": "Edit",
  "tool_input": {
    "file_path": "/Users/dan/dev/population_matters/tools/Content Hub/src/features/influencers/InfluencerPicker.tsx",
    "old_string": "export interface InfluencerPickerProps {\n  influencers: Influencer[];\n  value: string | undefined;\n  onChange: (influencerId: string | undefined) => void;\n  showOnlyActive?: boolean;\n  label?: string;\n  className?: string;\n}\n\nexport const InfluencerPicker: React.FC<InfluencerPickerProps> = ({\n  influencers,\n  value,\n  onChange,\n  showOnlyActive = true,\n  label = 'Influencer',\n  className,\n}) => {\n  const filteredInfluencers = showOnlyActive\n    ? influencers.filter((i) => i.status === 'Direct Outreach' || i.status === 'Collaborate')\n    : influencers;\n\n  return (\n    <div className={className}>\n      {label && <Label className=\"mb-1\">{label}</Label>}\n      <select\n        value={value || ''}\n        onChange={(e) => onChange(e.target.value || undefined)}\n        className={cx(selectBaseClasses, 'w-full px-3 py-2')}\n      >\n        <option value=\"\">No influencer</option>\n        {filteredInfluencers.map((influencer) => (\n          <option key={influencer.id} value={influencer.id}>\n            {influencer.name} ({influencer.platform})\n          </option>\n        ))}\n      </select>\n    </div>\n  );\n};",
    "new_string": "export interface InfluencerPickerProps {\n  influencers: Influencer[];\n  value: string | undefined;\n  onChange: (influencerId: string | undefined) => void;\n  onCreateNew?: () => void;\n  showOnlyActive?: boolean;\n  label?: string;\n  className?: string;\n}\n\nexport const InfluencerPicker: React.FC<InfluencerPickerProps> = ({\n  influencers,\n  value,\n  onChange,\n  onCreateNew,\n  showOnlyActive = true,\n  label = 'Influencer',\n  className,\n}) => {\n  const filteredInfluencers = showOnlyActive\n    ? influencers.filter((i) => i.status === 'Direct Outreach' || i.status === 'Collaborate')\n    : influencers;\n\n  return (\n    <div className={className}>\n      {label && (\n        <div className=\"mb-1 flex items-center justify-between\">\n          <Label>{label}</Label>\n          {onCreateNew && (\n            <button\n              type=\"button\"\n              onClick={onCreateNew}\n              className=\"text-xs font-medium text-ocean-600 hover:text-ocean-400\"\n            >\n              + New influencer\n            </button>\n          )}\n        </div>\n      )}\n      <select\n        value={value || ''}\n        onChange={(e) => onChange(e.target.value || undefined)}\n        className={cx(selectBaseClasses, 'w-full px-3 py-2')}\n      >\n        <option value=\"\">No influencer</option>\n        {filteredInfluencers.map((influencer) => (\n          <option key={influencer.id} value={influencer.id}>\n            {influencer.name} ({influencer.platform})\n          </option>\n        ))}\n      </select>\n    </div>\n  );\n};",
    "replace_all": false
  }
}
